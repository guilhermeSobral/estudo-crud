package com.crudcompleto.service;

import java.util.function.Consumer;

public interface CrudService {
	
	public default <T,ID> void delete(Consumer<T> consumer, T repository) {
		consumer.accept(repository);
	}

}
