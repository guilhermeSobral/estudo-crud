package com.crudcompleto.service;

import java.util.function.Consumer;

public interface CrudService {
	
	public default <T,ID> void delete(Consumer<T> consumer, T repository, ID id) {
		consumer.accept(repository);
	}

}
